// C Program to calculate the Area of Rectangle using the Hard Coded Inputs
#include<stdio.h>
int main() {
    float length=6.5;
    float breadth=2.4;
    printf("Area is %f \n",length*breadth);
    printf("Perimeter is %f \n",2*(length+breadth));
    return 0;
}
